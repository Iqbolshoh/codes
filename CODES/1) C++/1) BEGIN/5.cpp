#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int a, V, S;
	cout << " a = ";
	cin >> a;
	V = pow(a, 3);
	S = 6 * pow(a, 2);
	cout << " HAJM = " << V << endl;
	cout << " YUZA = " << S << endl;
	return 0;
}