#include <iostream>

using namespace std;

int main()
{
    float a, b, x;
    cout << " A = ";
    cin >> a;
    cout << " B = ";
    cin >> b;
    x = -b / a;
    cout << " X = " << x;
    return 0;
}