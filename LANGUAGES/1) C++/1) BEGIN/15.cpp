#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    float D, R, S;
    cout << " S = ";
    cin >> S;
    R = sqrt(S / M_PI);
    D = 2 * R;
    cout << " RADIUS = " << R << endl;
    cout << " DIAMETR = " << D << endl;
    return 0;
}
