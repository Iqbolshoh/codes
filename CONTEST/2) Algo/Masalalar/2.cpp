#include <bits/stdc++.h>

using namespace std;

int main()
{
    float a, b, c, d, e, f;
    cin >> a >> b >> c;
    d = pow(a, 2) * M_PI;
    e = pow(b, 2) * M_PI;
    f = pow(c, 2) * M_PI;
    printf("%.2f", d);
    cout << endl;
    printf("%.2f", e);
    cout << endl;
    printf("%.2f", f);
    return 0;
}
